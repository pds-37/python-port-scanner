# python-port-scanner
A simple Python-based port scanner that scans open ports on a target IP address or domain name.  The tool uses Python's socket library to attempt connections to ports and identify open services.  It also includes basic banner grabbing to detect service information running on open ports.
